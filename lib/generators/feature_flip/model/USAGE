Description:
    Generates the Feature database model class for FeatureFlip.

Example:
    rails generate feature_flip:model

    This will create:
        app/models/feature.rb (class Feature < ActiveRecord::Base)
